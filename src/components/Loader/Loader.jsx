import { ThreeDots } from "react-loader-spinner";

const Loader = () => {
  return (
    <div className="loader">
      <ThreeDots color="#b43583" height={64} width={64} />
    </div>
  );
};

export default Loader;
